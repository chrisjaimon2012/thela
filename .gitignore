node_modules/
dist/
.astro/
.wrangler/

# Local secrets. Cloudflare's own guidance is to gitignore both patterns.
.dev.vars
.dev.vars.*
.env
.env.*
!.env.example

# Real bank alert samples may contain an account number or balance.
# Redacted samples belong in docs/bank-samples/ and are committed deliberately.
docs/bank-samples/raw/

.DS_Store
*.log
.astro/
